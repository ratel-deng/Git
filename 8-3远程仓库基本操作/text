从远程仓库克隆到本地：
git clone 地址(添加了ssh则用ssh地址，没有则用http地址)

git remote -v
查看远程地址

git remote add 别名 地址
为远程仓库地址设置别名

git fetch 地址
仅仅拉取远程仓库更新，不合并到本地
git rebase origin/master
快进式合并到本地分支

git pull origin master
拉取远程更新并合并提交到本地

git push origin master:master
推送分支到远程
git push -f origin master
强制推送并覆盖远程仓库

git remote rm 别名
删除远程仓库名

git remote show origin
详细查看远程情况

git remote rename old别名 new别名
重命名地址别名
